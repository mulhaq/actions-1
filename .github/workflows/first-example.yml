name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v6
     

        - name: Welcome Message
          run: echo "My First GH Action Job"

        - name: List Files
          run: ls

        - name: Read File
          run: cat README.md


           